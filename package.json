{
  "name": "AngularBlog",
  "private": true,
  "version": "1.0.0",
  "description": "Design and implement a simple Web-based application, e.g. blog",
  "repository": "https://github.com/Atanasov86/AngularBlog",
  "license": "MIT",
  "devDependencies": {
    "bower": "^1.7.7",
    "http-server": "^0.9.0",
    "protractor": "^3.2.2"
  },
  "scripts": {
    "postinstall": "bower install",

    "prestart": "npm install",
    "start": "http-server -a localhost -p 8000 -c-1 ./app",

    "preupdate-webdriver": "npm install",
    "update-webdriver": "webdriver-manager update"
  }
}
